[user]
	email = $GITHUB_EMAIL
	name = $GITHUB_NAME
	signingkey = githubcommitsigningkey

[core]
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true  # use n and N to move between diff sections
  dark = true      # or light = true, or omit for auto-detection
  side-by-side = true
  line-numbers = true

[merge]
  conflictStyle = zdiff3

[push]
	autoSetupRemote = true
[init]
	defaultBranch = main
[commit]
	gpgsign = true
